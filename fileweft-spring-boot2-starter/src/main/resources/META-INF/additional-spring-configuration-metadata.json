{
  "properties": [
    {
      "name": "fileweft.default-tenant-enabled",
      "type": "java.lang.Boolean",
      "description": "Explicitly enables the fixed single-tenant fallback when no TenantProvider bean is configured. Leave disabled for production multi-tenant deployments.",
      "defaultValue": false
    },
    {
      "name": "fileweft.default-tenant-id",
      "type": "java.lang.String",
      "description": "Tenant identifier used only when fileweft.default-tenant-enabled is true. It must be configured explicitly."
    },
    {
      "name": "fileweft.storage.local-enabled",
      "type": "java.lang.Boolean",
      "description": "Explicitly enables process-local filesystem storage when no customer or plugin StorageAdapter is available. Leave disabled for durable production storage.",
      "defaultValue": false
    },
    {
      "name": "fileweft.storage.local-root",
      "type": "java.lang.String",
      "description": "Filesystem root used only when fileweft.storage.local-enabled is true. It must be configured explicitly."
    },
    {
      "name": "fileweft.persistence.migration-mode",
      "type": "ai.icen.fw.persistence.migration.FileWeftMigrationMode",
      "description": "Controls FileWeft-owned Flyway startup behavior. DISABLED performs no migration database access, VALIDATE verifies an existing FileWeft schema, and MIGRATE applies pending FileWeft migrations. Enabled modes require exactly one DataSource unless the host supplies an explicit FlywayMigrationRunner. The default is DISABLED.",
      "defaultValue": "DISABLED"
    },
    {
      "name": "fileweft.persistence.schema",
      "type": "java.lang.String",
      "description": "Explicit FileWeft schema required when migration mode is VALIDATE or MIGRATE. Blank values, implicit schema selection, and leading or trailing whitespace are rejected for enabled Starter migration modes."
    },
    {
      "name": "fileweft.persistence.create-schema",
      "type": "java.lang.Boolean",
      "description": "Allows MIGRATE to create the explicitly configured FileWeft schema. A true value is rejected in DISABLED and VALIDATE modes without database access; leave false when schema ownership belongs to the database operator.",
      "defaultValue": false
    },
    {
      "name": "fileweft.persistence.kingbase-flyway-compatibility-enabled",
      "type": "java.lang.Boolean",
      "description": "Adapts only Spring Boot Flyway's selected KingbaseES DataSource to the PostgreSQL-compatible Flyway engine while leaving the application's main DataSource unchanged. Disable only when the host supplies an equivalent Kingbase Flyway integration.",
      "defaultValue": true
    },
    {
      "name": "fileweft.compatibility.legacy-agent-autoconfiguration-enabled",
      "type": "java.lang.Boolean",
      "description": "Compatibility-only bridge that restores the legacy Agent repository, task, confirmation, and Doctor auto-configuration. It is disabled by default and must not be used as a new Agent product-support contract.",
      "defaultValue": false
    }
  ]
}
